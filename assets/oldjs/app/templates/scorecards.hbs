{{#if isLoaded}}
  <div class="navbar">
    <div class="navbar-inner scorecards">
      <ul class="nav">
        {{#each row in matrix}}
          {{#if hasOneChild}}
            {{ partial "scorecards/nav_link" }}
          {{else}}
            {{ partial "scorecards/dropdown" }}
          {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
{{/if}}